<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/page_goals_header"
            style="@style/UpdateHeader"
            android:text="@string/page_goals" />

        <ListView
            android:id="@+id/goals_page_goals"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/book_goals_header"
            style="@style/UpdateHeader"
            android:text="@string/book_goals" />

        <ListView
            android:id="@+id/goals_book_goals"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>

</ScrollView>
